import scrappersByName from './scrappers'
import { getScrap } from './utils'

export { getScrap,
  scrappersByName
}
const scrapDansTesMains = { getScrap,
  scrappersByName
}
export default scrapDansTesMains
